# 全站中英文国际化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 使用 `next-intl` 为全站 24 个页面、55+ 组件添加中英文 URL 路由国际化支持，提取约 630+ 条硬编码字符串到翻译文件。

**Architecture:** 基于 `next-intl` 的 App Router 方案：所有页面移入 `app/[locale]/` 目录，middleware 自动重定向 `/` → `/en`，翻译文件按命名空间组织在 `messages/{locale}.json`，Server/Client Components 分别使用 `getTranslations` / `useTranslations`。

**Tech Stack:** TypeScript, React 19, Next.js 16 App Router, `next-intl` v4

---

## 文件结构

### 新建文件
| 路径 | 职责 |
|------|------|
| `web/messages/en.json` | 英文翻译文件（全部命名空间） |
| `web/messages/zh.json` | 中文翻译文件（全部命名空间） |
| `web/src/i18n/routing.ts` | locale 路由配置 |
| `web/src/i18n/request.ts` | Server Component 翻译配置 |
| `web/src/i18n/navigation.ts` | i18n-aware 的 Link/usePathname/useRouter |
| `web/src/middleware.ts` | Next.js middleware |
| `web/src/components/locale-switcher.tsx` | 语言切换器组件 |
| `web/src/components/locale-switcher.test.tsx` | 语言切换器测试 |

### 移动到 `[locale]/` 的文件
所有 `web/src/app/` 下的文件（favicon.ico 除外）

### 修改的文件
- `web/package.json`
- `web/next.config.ts`
- `web/src/app/[locale]/layout.tsx`
- 所有页面和组件文件（使用 `useTranslations` 替换硬编码）
- `web/src/lib/public-content.ts`
- 相关测试文件

---

## Task 1: 安装 next-intl 并创建 i18n 基础设施

**Files:**
- Modify: `web/package.json`
- Modify: `web/next.config.ts`
- Create: `web/src/i18n/routing.ts`
- Create: `web/src/i18n/request.ts`
- Create: `web/src/i18n/navigation.ts`
- Create: `web/src/middleware.ts`

- [ ] **Step 1: 安装 next-intl**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/web
npm install next-intl
```

- [ ] **Step 2: 创建 `src/i18n/routing.ts`**

```typescript
import { defineRouting } from "next-intl/routing";

export const routing = defineRouting({
  locales: ["en", "zh"],
  defaultLocale: "en",
});
```

- [ ] **Step 3: 创建 `src/i18n/request.ts`**

```typescript
import { getRequestConfig } from "next-intl/server";
import { hasLocale } from "next-intl";
import { routing } from "./routing";

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale = hasLocale(routing.locales, requested)
    ? requested
    : routing.defaultLocale;

  return {
    locale,
    messages: (await import(`../../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 4: 创建 `src/i18n/navigation.ts`**

```typescript
import { createNavigation } from "next-intl/navigation";
import { routing } from "./routing";

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```

- [ ] **Step 5: 创建 `src/middleware.ts`**

```typescript
import createMiddleware from "next-intl/middleware";
import { routing } from "./i18n/routing";

export default createMiddleware(routing);

export const config = {
  matcher: ["/((?!api|_next|.*\\..*).*)"],
};
```

- [ ] **Step 6: 修改 `next.config.ts`**

```typescript
import type { NextConfig } from "next";
import createNextIntlPlugin from "next-intl/plugin";

const withNextIntl = createNextIntlPlugin("./src/i18n/request.ts");

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "**" },
      { protocol: "http", hostname: "**" },
    ],
  },
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 7: 创建最小翻译文件以通过编译**

创建 `messages/en.json`：
```json
{
  "Common": {
    "signIn": "Sign in",
    "exploreAssets": "Explore Assets",
    "backToAssets": "Back to Assets"
  }
}
```

创建 `messages/zh.json`：
```json
{
  "Common": {
    "signIn": "登录",
    "exploreAssets": "探索资产",
    "backToAssets": "返回资产库"
  }
}
```

- [ ] **Step 8: 验证 next-intl 基础编译**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/web
npx tsc --noEmit --project tsconfig.json 2>&1 | head -20
```

- [ ] **Step 9: 提交**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah
git add web/package.json web/package-lock.json web/next.config.ts web/src/i18n/ web/src/middleware.ts web/messages/
git commit -m "feat(web): install next-intl and create i18n infrastructure"
```

---

## Task 2: 移动 app 文件到 [locale]/ 目录

**Files:** 所有 `web/src/app/` 下的文件移动到 `web/src/app/[locale]/`

- [ ] **Step 1: 创建 [locale] 目录并移动文件**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/web/src/app
mkdir -p "[locale]"

# 移动根级文件（保留 favicon.ico）
mv layout.tsx "[locale]/"
mv page.tsx "[locale]/"
mv globals.css "[locale]/"
mv not-found.tsx "[locale]/"
mv not-found.test.tsx "[locale]/"
mv page.test.tsx "[locale]/"
mv layout.test.tsx "[locale]/"

# 移动所有子目录
mv about "[locale]/"
mv admin "[locale]/"
mv architecture "[locale]/"
mv assets "[locale]/"
mv auth "[locale]/"
mv community "[locale]/"
mv insights "[locale]/"
mv scenarios "[locale]/"
```

- [ ] **Step 2: 验证文件结构**

```bash
ls -la /Users/weiwei.g.zhang/Documents/hsah/web/src/app/
ls -la "/Users/weiwei.g.zhang/Documents/hsah/web/src/app/[locale]/"
```

预期：`app/` 下只有 `favicon.ico` 和 `[locale]/`，所有页面在 `[locale]/` 内。

- [ ] **Step 3: 提交**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah
git add web/src/app/
git commit -m "refactor(web): move all app files into [locale] directory"
```

---

## Task 3: 修改 RootLayout 接入 NextIntlClientProvider

**Files:**
- Modify: `web/src/app/[locale]/layout.tsx`

- [ ] **Step 1: 修改 layout.tsx**

将当前的 `layout.tsx` 替换为：

```typescript
import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import { NextIntlClientProvider, useMessages } from "next-intl";
import { notFound } from "next/navigation";
import { hasLocale } from "next-intl";
import { routing } from "@/i18n/routing";
import "./globals.css";
import { QueryProvider } from "@/components/query-provider";
import { ThemeProvider } from "@/components/theme-provider";

const inter = Inter({
  variable: "--font-sans",
  subsets: ["latin"],
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-mono",
  subsets: ["latin"],
});

export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>;
}): Promise<Metadata> {
  const { locale } = await params;
  const t = await import("next-intl/server").then((m) =>
    m.getTranslations({ locale, namespace: "Meta" }),
  );

  return {
    title: t("siteTitle"),
    description: t("siteDescription"),
  };
}

export default async function RootLayout({
  children,
  params,
}: Readonly<{
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}>) {
  const { locale } = await params;
  if (!hasLocale(routing.locales, locale)) notFound();

  const messages = await import(`../../../messages/${locale}.json`).then(
    (m) => m.default,
  );

  return (
    <html
      lang={locale}
      suppressHydrationWarning
      className={`${inter.variable} ${jetbrainsMono.variable} h-full antialiased`}
    >
      <body className="min-h-full bg-background text-foreground flex flex-col">
        <NextIntlClientProvider messages={messages}>
          <ThemeProvider>
            <QueryProvider>{children}</QueryProvider>
          </ThemeProvider>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 2: 提交**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah
git add "web/src/app/[locale]/layout.tsx"
git commit -m "feat(web): integrate NextIntlClientProvider into RootLayout"
```

---

## Task 4: 创建完整的翻译文件

**Files:**
- Modify: `web/messages/en.json`
- Modify: `web/messages/zh.json`

这是最大的单个 Task。需要根据扫描结果创建完整的翻译文件。

- [ ] **Step 1: 创建完整的 `messages/en.json`**

基于扫描结果，创建包含以下命名空间的完整英文翻译文件：

```json
{
  "Meta": {
    "siteTitle": "Hyperscaler Asset Hub",
    "siteDescription": "Explore scenario-driven AI demos, reusable architectures, and hyperscaler assets across AWS, Azure, and Google Cloud."
  },
  "Common": {
    "signIn": "Sign in",
    "exploreAssets": "Explore Assets",
    "backToAssets": "Back to Assets",
    "openLibrary": "Open Library",
    "brandName": "HYPERSCALER ASSET HUB",
    "brandTagline": "AI delivery content platform",
    "navAssets": "Assets",
    "navScenarios": "Scenarios",
    "navArchitecture": "Architecture",
    "navInsights": "Insights",
    "navCommunity": "Community",
    "navAbout": "About",
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete",
    "edit": "Edit",
    "submit": "Submit",
    "back": "Back",
    "next": "Next",
    "previous": "Previous",
    "loading": "Loading…",
    "confirm": "Confirm",
    "statusDraft": "draft",
    "statusPublished": "published",
    "statusArchived": "archived",
    "visibilityPublic": "public",
    "visibilityRestricted": "restricted",
    "visibilityInternal": "internal"
  },
  "Home": {
    "eyebrow": "HSAH PLATFORM",
    "heroTitle": "A branded content platform for AI delivery assets",
    "heroSummary": "Move from discovery to reuse through scenarios, architectures, insights, and implementation-ready assets with one shared public shell.",
    "exploreCta": "Explore Asset Library",
    "overviewCta": "See Platform Overview",
    "metricAssetsValue": "150+",
    "metricAssetsLabel": "Solution assets and demos",
    "metricIndustriesValue": "12",
    "metricIndustriesLabel": "Industry lenses and operating contexts",
    "metricLanesValue": "4",
    "metricLanesLabel": "Role-based content lanes",
    "metricEntryPointValue": "1",
    "metricEntryPointLabel": "Unified public entry point",
    "platformValueEyebrow": "PLATFORM VALUE",
    "valueTitle": "Align stakeholders, then move into implementation detail.",
    "valueDescription": "The homepage now frames the product, exposes real featured assets, and sends every role into the most relevant content lane before they enter the library.",
    "goToAssetsCta": "Go to Asset Library",
    "browseScenariosCta": "Browse Scenarios"
  },
  "Assets": {
    "eyebrow": "ASSET LIBRARY",
    "title": "Asset Library",
    "summary": "Search reusable demos, architectures, and implementation references.",
    "searchPlaceholder": "Search by title or description",
    "searchAssetsLabel": "Search assets",
    "searchAssetsAriaLabel": "Search assets",
    "sortTitle": "Title",
    "sortRecentlyUpdated": "Recently updated",
    "savedFilters": "Saved filters",
    "resultsCount": "{count} results",
    "clearFilters": "Clear filters",
    "applyFilters": "Apply filters",
    "sortByLabel": "Sort by",
    "sortByAriaLabel": "Sort by",
    "gridViewAriaLabel": "Grid view",
    "listViewAriaLabel": "List view",
    "cloudLabel": "Cloud",
    "cloudAriaLabel": "Cloud",
    "allClouds": "All clouds",
    "industryLabel": "Industry",
    "industryAriaLabel": "Industry",
    "allIndustries": "All industries",
    "technologyLabel": "Technology",
    "technologyAriaLabel": "Technology",
    "allTechnologies": "All technologies",
    "assetTypeLabel": "Asset type",
    "assetTypeAriaLabel": "Asset type",
    "allAssetTypes": "All asset types",
    "emptyTitle": "No assets matched the current filters.",
    "emptyDescription": "Clear the active filters or return to featured assets to see all available content.",
    "emptyClearFilters": "Clear filters",
    "emptyBrowseAll": "Browse all",
    "viewDetails": "View details",
    "paginationInfo": "Page {currentPage} of {totalPages}",
    "previousPage": "Previous",
    "previousPageAriaLabel": "Previous page",
    "nextPage": "Next",
    "nextPageAriaLabel": "Next page"
  },
  "AssetDetail": {
    "exploreAssets": "Explore Assets",
    "backToAssets": "Back to Assets",
    "sharedContext": "Shared context",
    "useCases": "Use cases",
    "watchDemoVideo": "Watch demo video",
    "openLiveDemo": "Open live demo",
    "salesValue": "Sales value",
    "differentiators": "Differentiators",
    "outcomes": "Outcomes",
    "implementation": "Implementation",
    "prerequisites": "Prerequisites",
    "rolloutSteps": "Rollout steps",
    "viewMode": "View mode",
    "chooseLensTitle": "Choose the lens for this asset",
    "chooseLensDescription": "Sales focuses on value framing. Delivery reveals implementation guidance when access is granted.",
    "modeSales": "Sales",
    "modeDelivery": "Delivery",
    "controlledContent": "Controlled delivery content",
    "deliveryAccessTitle": "Delivery implementation access",
    "deliveryAccessSigninDesc": "Sign in with an approved role to review runbooks, deployment steps, and delivery notes.",
    "deliveryAccessRequestDesc": "Request delivery access to review runbooks, deployment steps, and delivery notes.",
    "requestAccess": "Request access"
  },
  "VideoPlayer": {
    "eyebrow": "Demo video",
    "allVideos": "All videos",
    "primaryBadge": "Primary"
  },
  "NotFound": {
    "code": "404",
    "title": "Page not found",
    "description": "The page you are looking for does not exist or has been moved.",
    "backToHome": "Back to home",
    "exploreAssets": "Explore assets"
  },
  "Login": {
    "brandName": "Hyperscaler Asset Hub",
    "heroTitle": "Explore the public content platform, then move into operational workspaces.",
    "heroDescription": "Access saved assets, profiles, and admin operations from one identity entry point.",
    "featurePublicTitle": "Public discovery",
    "featurePublicDesc": "Browse reusable demos, architectures, and scenario-led content before you sign in.",
    "featureContinuityTitle": "Operational continuity",
    "featureContinuityDesc": "Move from discovery into admin workflows without losing route context.",
    "featureProtectedTitle": "Protected access",
    "featureProtectedDesc": "Keep sensitive governance actions behind authenticated workspaces and 2FA when required.",
    "quickLookLabel": "Need a quick look first?",
    "explorePlatformCta": "Explore the public content platform",
    "gatewayEyebrow": "Identity Gateway",
    "signInTitle": "Sign in",
    "passwordStepDescription": "Access saved assets, profiles, and admin operations.",
    "totpStepDescription": "Enter the 6-digit code from your authenticator to continue.",
    "adminTitle": "Admin workspace sign-in",
    "adminDescription": "You were sent here from the admin workspace and will return to the exact page after sign-in.",
    "workspaceLabel": "Workspace",
    "workspaceValue": "Admin workspace",
    "targetPathLabel": "Target path",
    "returnRouteNote": "Return route: the public library remains your discovery surface for assets, profiles, and reference content.",
    "emailLabel": "Email",
    "emailPlaceholder": "name@company.com",
    "passwordLabel": "Password",
    "passwordPlaceholder": "••••••••",
    "totpLabel": "2FA Code",
    "totpPlaceholder": "123456",
    "verifyAndSignIn": "Verify & Sign in",
    "signInButton": "Sign in",
    "createAccount": "Create account (dev)",
    "returnToLibrary": "Return to public library",
    "fallbackEyebrow": "IDENTITY GATEWAY",
    "fallbackTitle": "Loading sign-in workspace...",
    "errorLoginFailed": "Login failed",
    "errorCreateAccountFailed": "Account creation failed"
  },
  "About": {
    "eyebrow": "ABOUT",
    "heroTitle": "A public content layer that helps teams align before they commit to build.",
    "heroSummary": "The hub connects scenario framing, architecture direction, editorial insight, and community participation so teams can move from curiosity to execution with less ambiguity.",
    "joinCommunity": "Join Community",
    "exploreAssets": "Explore Assets",
    "readInsights": "Read Insights",
    "howUsedTitle": "How the platform is used",
    "howUsedItem1": "Stakeholders browse scenarios to align on business value and workflow change.",
    "howUsedItem2": "Architecture teams use reference patterns to reduce design churn before delivery begins.",
    "howUsedItem3": "Program leaders share insight articles to align controls, ownership, and rollout expectations.",
    "howUsedItem4": "Working groups and labs create a repeatable feedback loop across teams and sectors.",
    "relatedLinksTitle": "Start from the section that fits your need",
    "linkScenariosLabel": "Start with scenarios",
    "linkScenariosDesc": "Use business journeys when the problem statement is still forming.",
    "linkArchitectureLabel": "Start with architecture",
    "linkArchitectureDesc": "Use reference views when the delivery model and controls need alignment.",
    "linkCommunityLabel": "Start with community",
    "linkCommunityDesc": "Use programs and labs when teams need peer exchange and practical review.",
    "linkAssetsLabel": "Go to assets",
    "linkAssetsDesc": "Use the asset library when the team is ready for implementation-oriented exploration."
  },
  "Footer": {
    "aboutHeading": "ABOUT THE HUB",
    "aboutDescription": "Discover scenario-led content, reference architecture thinking, and implementation-ready delivery assets from one branded platform.",
    "sectionsHeading": "SECTIONS",
    "startHereHeading": "START HERE",
    "assetLibrary": "Asset Library",
    "aboutPlatform": "About the platform"
  },
  "ContentLanes": {
    "eyebrow": "CONTENT LANES",
    "title": "Choose your path",
    "description": "Start with the lens that matches your role, then move deeper into assets, decisions, and reuse patterns.",
    "openLane": "Open lane"
  },
  "FeaturedAssets": {
    "eyebrow": "CURATED PICKS",
    "title": "Featured assets",
    "description": "Jump straight into reusable reference assets that frame trust, orchestration, and knowledge delivery.",
    "viewDetails": "View asset details"
  },
  "Theme": {
    "switchToLight": "Switch to light mode",
    "switchToDark": "Switch to dark mode",
    "light": "Light",
    "dark": "Dark"
  },
  "Error": {
    "somethingWentWrong": "Something went wrong",
    "unexpectedMessage": "An unexpected error occurred. Please try reloading the page or return home.",
    "detailsLabel": "Error details",
    "reloadPage": "Reload page",
    "backToHome": "Back to home"
  },
  "RouteGuard": {
    "checkingSession": "Checking your session before loading admin tools.",
    "signInRequired": "You need to sign in before accessing admin routes."
  },
  "PublicSite": {
    "openLibrary": "Open Library",
    "openDetails": "Open details"
  },
  "FilterToolbar": {
    "filters": "Filters"
  },
  "Admin": {
    "...": "(Admin 命名空间约 200+ key，见独立 Admin 翻译文件)"
  },
  "Nav": {
    "scenarios": "Scenarios",
    "architecture": "Architecture",
    "insights": "Insights",
    "community": "Community",
    "about": "About"
  }
}
```

- [ ] **Step 2: 创建完整的 `messages/zh.json`**

对应的中文翻译文件，结构完全一致，值替换为中文。

- [ ] **Step 3: 补充 Admin 命名空间完整 key**

Admin 命名空间包含约 200+ key，覆盖：
- `Admin.overview.*` — Dashboard KPI 和概述
- `Admin.quickAction.*` — 快捷操作
- `Admin.recentDrafts.*` — 近期草稿
- `Admin.kpi.*` — KPI 卡片
- `Admin.flow.*` / `Admin.actions.*` / `Admin.governance.*` — 分区标题
- `Admin.assets.*` — 资产管理（列名、操作、状态、对话框、toast）
- `Admin.roles.*` — 角色管理
- `Admin.users.*` — 用户管理
- `Admin.templates.*` — 模板管理
- `Admin.policies.*` — 策略管理
- `Admin.policyWizard.*` — 策略向导
- `Admin.matrix.*` — 权限矩阵
- `Admin.simulator.*` — 模拟器
- `Admin.assetEditor.*` — 资产编辑表单（所有字段、placeholder、区块标题）
- `Admin.videoManager.*` — 视频管理器
- `Admin.shell.*` / `Admin.sidebar.*` / `Admin.topbar.*` — 壳层组件
- `Admin.layout.*` — 布局

- [ ] **Step 4: 补充 public-content.ts 的翻译 key**

`public-content.ts` 中有约 120+ 条内容数据（scenarios、architectures、insights、community、about highlights），需要提取到翻译文件中。

- [ ] **Step 5: 验证翻译文件 JSON 语法**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/web
python -c "import json; json.load(open('messages/en.json')); print('en.json OK')"
python -c "import json; json.load(open('messages/zh.json')); print('zh.json OK')"
```

- [ ] **Step 6: 提交**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah
git add web/messages/
git commit -m "feat(web): create complete en/zh translation files with all namespaces"
```

---

## Task 5: 创建语言切换器组件

**Files:**
- Create: `web/src/components/locale-switcher.tsx`
- Create: `web/src/components/locale-switcher.test.tsx`

- [ ] **Step 1: 编写语言切换器测试**

```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, it, expect, vi } from "vitest";
import { LocaleSwitcher } from "./locale-switcher";

const mockReplace = vi.fn();
vi.mock("@/i18n/navigation", () => ({
  usePathname: () => "/assets",
  useRouter: () => ({ replace: mockReplace }),
}));

describe("LocaleSwitcher", () => {
  it("shows 中文 button when current locale is en", () => {
    render(<LocaleSwitcher locale="en" />);
    expect(screen.getByText("中文")).toBeTruthy();
  });

  it("shows EN button when current locale is zh", () => {
    render(<LocaleSwitcher locale="zh" />);
    expect(screen.getByText("EN")).toBeTruthy();
  });

  it("calls router.replace with zh when clicking from en", () => {
    render(<LocaleSwitcher locale="en" />);
    fireEvent.click(screen.getByText("中文"));
    expect(mockReplace).toHaveBeenCalledWith("/assets", { locale: "zh" });
  });
});
```

- [ ] **Step 2: 实现语言切换器**

```tsx
"use client";

import { usePathname, useRouter } from "@/i18n/navigation";
import { Globe } from "lucide-react";

type LocaleSwitcherProps = {
  locale: string;
};

export function LocaleSwitcher({ locale }: LocaleSwitcherProps) {
  const pathname = usePathname();
  const router = useRouter();

  function switchLocale() {
    const target = locale === "en" ? "zh" : "en";
    router.replace(pathname, { locale: target });
  }

  return (
    <button
      type="button"
      onClick={switchLocale}
      className="inline-flex items-center gap-1.5 text-sm text-muted-foreground transition-colors hover:text-foreground"
      aria-label={locale === "en" ? "Switch to Chinese" : "Switch to English"}
    >
      <Globe className="size-4" />
      {locale === "en" ? "中文" : "EN"}
    </button>
  );
}
```

- [ ] **Step 3: 运行测试**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/web
npx vitest run src/components/locale-switcher.test.tsx
```

- [ ] **Step 4: 提交**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah
git add web/src/components/locale-switcher.tsx web/src/components/locale-switcher.test.tsx
git commit -m "feat(web): add LocaleSwitcher component"
```

---

## Task 6: 集成语言切换器到 Header + 更新公共组件

**Files:**
- Modify: `web/src/components/public/public-header.tsx`
- Modify: `web/src/components/public/public-footer.tsx`
- Modify: `web/src/components/theme-toggle.tsx`
- Modify: `web/src/components/asset-detail-view.tsx`
- Modify: `web/src/components/asset-video-player.tsx`
- Modify: `web/src/components/error-boundary.tsx`
- Modify: `web/src/components/route-guard.tsx`
- Modify: `web/src/components/public-site-shell.tsx`
- Modify: `web/src/components/public/content-lane-grid.tsx`
- Modify: `web/src/components/public/homepage-featured-assets.tsx`
- Modify: `web/src/components/product/filter-toolbar.tsx`

- [ ] **Step 1: 修改 public-header.tsx**

添加 `LocaleSwitcher` 导入，使用 `useTranslations("Common")` 替换硬编码字符串。

关键变更：
- `"HYPERSCALER ASSET HUB"` → `t("brandName")`
- `"AI delivery content platform"` → `t("brandTagline")`
- `"Assets"` → `t("navAssets")`
- `"Sign in"` → `t("signIn")`
- 将 `<Link href="/">` 替换为 i18n-aware 的 `Link`
- 在 Header 右侧区域添加 `<LocaleSwitcher locale={locale} />`

- [ ] **Step 2: 修改 public-footer.tsx**

使用 `useTranslations("Footer")` 和 `useTranslations("Common")`。

- [ ] **Step 3: 修改 theme-toggle.tsx**

使用 `useTranslations("Theme")` 替换 aria-label。

- [ ] **Step 4: 修改 asset-detail-view.tsx**

使用 `useTranslations("AssetDetail")` 和 `useTranslations("VideoPlayer")`。

- [ ] **Step 5: 修改 asset-video-player.tsx**

使用 `useTranslations("VideoPlayer")`。

- [ ] **Step 6: 修改其他组件**

`error-boundary.tsx`、`route-guard.tsx`、`public-site-shell.tsx`、`content-lane-grid.tsx`、`homepage-featured-assets.tsx`、`filter-toolbar.tsx` 各自使用对应的命名空间翻译。

- [ ] **Step 7: 运行相关测试**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/web
npx vitest run src/components/
```

- [ ] **Step 8: 提交**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah
git add web/src/components/
git commit -m "feat(web): migrate shared components to use useTranslations"
```

---

## Task 7: 迁移公共页面

**Files:**
- Modify: `web/src/app/[locale]/page.tsx`
- Modify: `web/src/app/[locale]/about/page.tsx`
- Modify: `web/src/app/[locale]/assets/page.tsx`
- Modify: `web/src/app/[locale]/assets/assets-client.tsx`
- Modify: `web/src/app/[locale]/assets/[slug]/page.tsx`
- Modify: `web/src/app/[locale]/auth/login/page.tsx`
- Modify: `web/src/app/[locale]/not-found.tsx`

- [ ] **Step 1: 迁移首页 `page.tsx`**

Server Component 使用 `getTranslations("Home")`。

- [ ] **Step 2: 迁移 about 页面**

使用 `getTranslations("About")`。

- [ ] **Step 3: 迁移 assets 页面和 assets-client**

`page.tsx` 是 Server Component 用 `getTranslations`。
`assets-client.tsx` 是 Client Component 用 `useTranslations("Assets")`。
注意动态插值：`` `${count} results` `` → `t("resultsCount", { count })`。

- [ ] **Step 4: 迁移 detail 页面**

使用 `getTranslations("AssetDetail")`。

- [ ] **Step 5: 迁移 login 页面**

使用 `useTranslations("Login")`。注意中英文混合的错误提示统一翻译。

- [ ] **Step 6: 迁移 not-found**

使用 `getTranslations("NotFound")`。

- [ ] **Step 7: 运行测试**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/web
npx vitest run "src/app/[locale]/"
```

- [ ] **Step 8: 提交**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah
git add "web/src/app/[locale]/"
git commit -m "feat(web): migrate public pages to use useTranslations"
```

---

## Task 8: 迁移管理后台页面和组件

**Files:**
- Modify: `web/src/app/[locale]/admin/page.tsx`
- Modify: `web/src/app/[locale]/admin/layout.tsx`
- Modify: `web/src/app/[locale]/admin/assets/page.tsx`
- Modify: `web/src/app/[locale]/admin/assets/new/page.tsx`
- Modify: `web/src/app/[locale]/admin/assets/[id]/edit/page.tsx`
- Modify: `web/src/app/[locale]/admin/roles/page.tsx`
- Modify: `web/src/app/[locale]/admin/users/page.tsx`
- Modify: `web/src/app/[locale]/admin/templates/page.tsx`
- Modify: `web/src/app/[locale]/admin/policies/page.tsx`
- Modify: `web/src/app/[locale]/admin/policies/wizard/page.tsx`
- Modify: `web/src/app/[locale]/admin/matrix/page.tsx`
- Modify: `web/src/app/[locale]/admin/simulator/page.tsx`
- Modify: `web/src/components/admin/asset-editor-form.tsx`
- Modify: `web/src/components/admin/asset-video-manager.tsx`
- Modify: `web/src/components/admin/admin-shell.tsx`
- Modify: `web/src/components/admin/admin-sidebar.tsx`
- Modify: `web/src/components/admin/admin-topbar.tsx`

- [ ] **Step 1: 迁移 admin 壳层组件**

`admin-shell.tsx`、`admin-sidebar.tsx`、`admin-topbar.tsx` 使用 `useTranslations("Admin")`。

- [ ] **Step 2: 迁移 admin 首页**

使用 `useTranslations("Admin")` 覆盖 KPI、操作、概述等。

- [ ] **Step 3: 迁移 admin/assets**

资产列表页使用 `useTranslations("Admin")` 覆盖列名、操作、对话框、toast。

- [ ] **Step 4: 迁移 admin/roles、admin/users、admin/templates**

各页面使用对应子命名空间翻译。

- [ ] **Step 5: 迁移 admin/policies 和 policy-wizard**

策略管理页和向导页使用 `useTranslations("Admin")`。

- [ ] **Step 6: 迁移 admin/matrix 和 admin/simulator**

权限矩阵和模拟器页面使用对应翻译。

- [ ] **Step 7: 迁移 asset-editor-form 和 asset-video-manager**

表单组件使用 `useTranslations("Admin")` 覆盖所有字段标签和 placeholder。

- [ ] **Step 8: 运行全量测试**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/web
npx vitest run
```

- [ ] **Step 9: 提交**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah
git add web/src/app/ web/src/components/
git commit -m "feat(web): migrate admin pages and components to use useTranslations"
```

---

## Task 9: 迁移 public-content.ts 内容数据

**Files:**
- Modify: `web/src/lib/public-content.ts`

- [ ] **Step 1: 将 public-content.ts 中的硬编码字符串改为翻译 key**

将所有静态内容数据（scenarios、architectures、insights、community、aboutHighlights、contentLanes、navLinks）中的硬编码字符串替换为翻译 key 引用。

由于 `public-content.ts` 是纯数据模块（非组件），需要：
1. 将数据函数改为接受 `t` 翻译函数参数
2. 或将数据提取到翻译文件中，组件通过 `useTranslations` 直接获取

推荐方案：将 `public-content.ts` 改为导出函数，接受 `t` 参数。

- [ ] **Step 2: 更新调用 public-content 的组件**

更新所有导入 `publicNavLinks`、`homepageContentLanes` 等的组件，改为调用翻译函数。

- [ ] **Step 3: 运行测试**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/web
npx vitest run src/lib/public-assets.test.ts src/app/
```

- [ ] **Step 4: 提交**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah
git add web/src/lib/public-content.ts web/src/
git commit -m "feat(web): migrate public-content.ts data to use translations"
```

---

## Task 10: 更新测试断言并全量验证

**Files:** 所有包含 UI 文本断言的测试文件

- [ ] **Step 1: 更新测试中的文本断言**

测试文件中引用翻译后文本的断言需要更新为匹配翻译后的实际值。对于使用 `@testing-library/react` 的测试，需要在测试环境中提供 `NextIntlClientProvider` mock。

- [ ] **Step 2: 运行全量前端测试**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/web
npx vitest run
```

- [ ] **Step 3: 运行后端测试确认无影响**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/api
DATABASE_URL='sqlite:///./test_i18n.db' python -m pytest tests/ -x -q
```

- [ ] **Step 4: 提交**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah
git add web/
git commit -m "fix(web): update test assertions for i18n migration"
```

---

## Task 11: 构建验证与端到端测试

**Files:** 无新增/修改文件

- [ ] **Step 1: 构建前端**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah/web
npm run build
```

预期：无 TypeScript 错误，无构建失败。

- [ ] **Step 2: 启动服务并验证英文页面**

```bash
PORT=3100 npm run start &
curl -s -o /dev/null -w "%{http_code}" http://localhost:3100/en
curl -s -o /dev/null -w "%{http_code}" http://localhost:3100/en/assets
curl -s -o /dev/null -w "%{http_code}" http://localhost:3100/en/assets/agentic-service-mesh-kubernetes
```

预期：全部返回 200。

- [ ] **Step 3: 验证中文页面**

```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3100/zh
curl -s -o /dev/null -w "%{http_code}" http://localhost:3100/zh/assets
```

预期：全部返回 200，页面内容为中文。

- [ ] **Step 4: 验证根路径重定向**

```bash
curl -s -o /dev/null -w "%{redirect_url}" http://localhost:3100/
```

预期：重定向到 `/en`。

- [ ] **Step 5: 浏览器验证语言切换**

打开 `http://localhost:3100/en`，点击语言切换按钮，确认页面切换到中文且 URL 变为 `/zh`。

- [ ] **Step 6: 提交最终合并**

```bash
cd /Users/weiwei.g.zhang/Documents/hsah
git add -A
git commit -m "feat: complete i18n migration for zh/en internationalization"
```
